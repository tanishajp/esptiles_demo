/// This class contains all URL which is being called to fetch repositories from server

class ApiClient {
  static String baseUrl = "http://esptiles.imperoserver.in/api/API/Product/";

  static String dashBoard = 'DashBoard';
  static String productList = 'ProductList';
}

